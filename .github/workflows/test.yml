name: Test

on:
  push:

jobs:
  test:
    name: Test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install -e .
      - run: python -m deltachat_test_wheels
